<UserControl x:Class="ZooManager.UI.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             AutomationProperties.AutomationId="View.Employees">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Linke Seite: Mitarbeiterliste -->
        <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,12,0">
            <DockPanel>
                <TextBlock Text="Personal" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>
                <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                    <Button Content="+ Mitarbeiter anlegen" Padding="8" Click="AddEmployee_Click" Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold"/>
                    <Button Content="Mitarbeiter löschen" Padding="8" Margin="0,8,0,0" Click="DeleteEmployee_Click" Background="#FFEBEE" Foreground="#D32F2F" BorderBrush="#FFCDD2"/>
                </StackPanel>
                <ListBox x:Name="EmployeesList" BorderThickness="0" SelectionChanged="EmployeesList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Korrigiert: Kein Komma mehr am Ende -->
                            <TextBlock FontSize="14" Margin="0,4">
                                <Run Text="{Binding LastName}" FontSize="16" FontWeight="Bold"/>, <Run Text="{Binding FirstName}" FontSize="16" FontWeight="SemiBold"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Rechte Seite -->
        <Grid Grid.Column="1">
            <!-- ANZEIGE-MODUS (Qualifikationen) -->
            <ScrollViewer x:Name="EmployeeDetailsArea" Visibility="Visible">
                <Border Style="{StaticResource Card}" Margin="0">
                    <StackPanel>
                        <TextBlock x:Name="SelectedEmployeeTitle" Text="Wählen Sie einen Mitarbeiter" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>
                        
                        <TextBlock Text="Qualifikationen:" FontSize="16" FontWeight="SemiBold" Margin="0,10,0,8"/>
                        <TextBlock Text="Kann folgende Tierarten betreuen:" Margin="0,0,0,12" FontStyle="Italic" Foreground="{StaticResource TextMutedBrush}"/>
                        
                        <ItemsControl x:Name="QualificationList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding SpeciesName}" 
                                              IsChecked="{Binding IsQualified}" 
                                              Margin="0,6"
                                              Click="QualificationCheckBox_Click"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <!-- EDITOR-MODUS (Neu anlegen) -->
            <Border x:Name="EmployeeEditorArea" Style="{StaticResource Card}" Visibility="Collapsed" VerticalAlignment="Top" Margin="0">
                <StackPanel>
                    <TextBlock Text="Mitarbeiter registrieren" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <Label Content="Vorname:"/>
                    <TextBox x:Name="NewEmpFirstName" Margin="0,0,0,12" Padding="8"/>

                    <Label Content="Nachname:"/>
                    <TextBox x:Name="NewEmpLastName" Margin="0,0,0,16" Padding="8"/>

                    <Separator Margin="0,8,0,16"/>

                    <TextBlock Text="Login für Mitarbeiter" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                    <Label Content="Benutzername (Login):"/>
                    <TextBox x:Name="NewEmpUsername" Margin="0,0,0,12" Padding="8"/>

                    <Label Content="Passwort:"/>
                    <PasswordBox x:Name="NewEmpPassword" Margin="0,0,0,24" Padding="8"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Abbrechen" Style="{StaticResource NavButton}" Margin="0,0,12,0" Click="CancelEmployeeEditor_Click"/>
                        <Button Content="Speichern" Padding="24,8" Background="{StaticResource PrimaryBrush}" Foreground="White" Click="SaveNewEmployee_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>