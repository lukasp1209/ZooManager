<UserControl x:Class="ZooManager.UI.Views.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel Margin="24">
        <TextBlock Text="Events" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
        <Border Style="{StaticResource Card}" Margin="0,24,0,0">
            <TextBlock Text="Anstehende Veranstaltungen im Zoo." Foreground="{StaticResource TextMutedBrush}"/>
        </Border>
    </StackPanel>
</UserControl>