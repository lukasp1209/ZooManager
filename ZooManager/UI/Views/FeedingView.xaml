<UserControl x:Class="ZooManager.UI.Views.FeedingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Linke Seite: Fütterungsliste -->
        <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,12,0">
            <DockPanel>
                <TextBlock Text="Fütterungsplan" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>
                <ListBox x:Name="FeedingList" BorderThickness="0" SelectionChanged="FeedingList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4">
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding NextFeedingTime, StringFormat='\{0:ddd, dd.MM. HH:mm\} Uhr'}" FontSize="12" Foreground="{StaticResource PrimaryBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Rechte Seite: Editor -->
        <Grid Grid.Column="1">
            <Border Style="{StaticResource Card}" VerticalAlignment="Top">
                <StackPanel x:Name="FeedingEditorArea" Visibility="Collapsed">
                    <TextBlock x:Name="SelectedAnimalTitle" Text="Tiername" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <Label Content="Geplante Fütterung ändern:"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <DatePicker x:Name="EditFeedingDate" Grid.Column="0" Margin="0,0,8,0" Padding="5"/>
                        <ComboBox x:Name="EditFeedingHour" Grid.Column="1" Margin="0,0,8,0" Padding="5"/>
                        <ComboBox x:Name="EditFeedingMinute" Grid.Column="2" Padding="5"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <Button Content="Uhrzeit speichern" Padding="20,10" Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold" Click="SaveFeedingTime_Click"/>
                        <Button Content="Jetzt gefüttert (+24h)" Margin="12,0,0,0" Padding="20,10" Background="{StaticResource AccentBrush}" Foreground="White" FontWeight="Bold" Click="FeedingDone_Click"/>
                    </StackPanel>
                    
                    <TextBlock Text="* 'Jetzt gefüttert' verschiebt den Termin um exakt 24 Stunden bei gleicher Uhrzeit." 
                               FontStyle="Italic" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
            
            <TextBlock x:Name="PlaceholderText" Text="Wählen Sie ein Tier aus der Liste aus, um die Fütterung zu bearbeiten." 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource TextMutedBrush}"/>
        </Grid>
    </Grid>
</UserControl>