<Window x:Class="ZooManager.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zoo Manager"
        Width="1200" Height="750"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F8">

    <!-- ========== LAYOUT ========== -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== HEADER ===== -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Zoo Manager"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    
                    <!-- User Info -->
                    <Border Background="#000000" 
                            CornerRadius="6" 
                            Padding="16,8"
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤" 
                                       FontSize="18" 
                                       Margin="0,0,8,0"
                                       VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock x:Name="UsernameText"
                                           Text="Benutzername"
                                           FontWeight="SemiBold"
                                           FontSize="14"
                                           Foreground="White"/>
                                <TextBlock x:Name="UserRoleText"
                                           Text="Rolle"
                                           FontSize="11"
                                           Foreground="#FFFFFFCC"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Logout Button -->
                    <Button x:Name="LogoutButton"
                            Content="Abmelden"
                            Height="36"
                            Padding="16,0"
                            Background="#000000"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="LogoutButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FFFFFF30"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== MAIN ===== -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- SIDEBAR -->
            <Border Grid.Column="0" Background="White">
                <StackPanel Margin="16">

                    <TextBlock Text="Navigation"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextMutedBrush}"
                               Margin="8,0,0,16"/>

                    <Button Content="Dashboard" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="Dashboard"/>
                    <Button Content="Tiere" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="Animals"/>
                    <Button Content="Tierart" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="Species"/>
                    <Button Content="Gehege" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="Enclosures"/>
                    <Button Content="Mitarbeiter" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="Employees"/>
                    <Button Content="Fütterung" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="FeedingPlan"/>
                    
                    <Separator Margin="0,16"/>
                    <Button Content="Events" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="Events"/>  
                    <Button Content="Statistik" Style="{StaticResource NavButton}" Click="NavButton_Click" Tag="Reports"/>
                </StackPanel>
            </Border>

            <!-- CONTENT -->
            <ScrollViewer Grid.Column="1">
                <ContentControl x:Name="MainContentPresenter" Margin="0"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>