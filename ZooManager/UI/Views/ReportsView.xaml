<UserControl x:Class="ZooManager.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             AutomationProperties.AutomationId="View.Reports">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Linke Seite: Berichtsauswahl -->
        <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,12,0">
            <StackPanel>
                <TextBlock Text="Berichte &amp; Statistik" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>
                <ListBox x:Name="ReportTypeList" BorderThickness="0" SelectionChanged="ReportTypeList_SelectionChanged">
                    <ListBoxItem Padding="0,4">
                        <StackPanel Margin="0,4">
                            <TextBlock Text="Bestand nach Tierart" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="Übersicht der Tierpopulation" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                        </StackPanel>
                    </ListBoxItem>
                    
                    <ListBoxItem Padding="0,4">
                        <StackPanel Margin="0,4">
                            <TextBlock Text="Gehege-Auslastung" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="Kapazitätsanalyse der Anlagen" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                        </StackPanel>
                    </ListBoxItem>
                    
                    <ListBoxItem Padding="0,4">
                        <StackPanel Margin="0,4">
                            <TextBlock Text="Personal-Qualifikationen" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="Verfügbarkeit von Fachexperten" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Rechte Seite: Analyse-Zentrum -->
        <Border Grid.Column="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
            
                <TextBlock x:Name="ReportTitle" Text="Wählen Sie einen Bericht" Grid.Row="0" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

                <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <DockPanel>
                        <TextBlock Text="Visuelle Analyse" DockPanel.Dock="Top" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{StaticResource TextMutedBrush}"/>
                        <ItemsControl x:Name="ChartItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="5" VerticalAlignment="Bottom"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Bottom" Margin="10,0">
                                        <Border Background="{StaticResource PrimaryBrush}" 
                                                Height="{Binding BarHeight}" 
                                                Width="30" 
                                                CornerRadius="4,4,0,0"
                                                ToolTip="{Binding ToolTipText}">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="5" Opacity="0.1" ShadowDepth="1"/>
                                            </Border.Effect>
                                        </Border>
                                        <TextBlock Text="{Binding Label}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0" TextWrapping="Wrap" MaxHeight="30"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DockPanel>
                </Border>

                <!-- DATENTABELLE -->
                <DataGrid x:Name="ReportDataGrid" Grid.Row="2" 
                          AutoGenerateColumns="True" 
                          IsReadOnly="True" 
                          Background="White" 
                          BorderThickness="1" 
                          BorderBrush="#EEE"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F9F9F9">
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>